name: Build Catalyst-26 A13 Exploit

on: 
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Create App Structure
        run: |
          mkdir -p Payload/A13Exploit.app
          
          # 1. Compilação do App Shell (Focado em A13/arm64e)
          # Adicionamos flags de otimização e min-version para evitar bloqueios do Kernel
          clang -arch arm64e -fobjc-arc -m64 \
          -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
          -miphoneos-version-min=15.0 \
          -framework UIKit -framework WebKit -framework Foundation -framework IOKit -framework CoreGraphics \
          ViewController.m main.m \
          -o Payload/A13Exploit.app/A13Exploit
          
          # 2. Copiar Recursos (HTML e o binário SSHD)
          # Certifique-se de que 'index.html' e 'sshd' estão na raiz do seu repo
          cp index.html Payload/A13Exploit.app/index.html
          cp sshd Payload/A13Exploit.app/sshd
          cp libkernrw.0.dylib Payload/A13Exploit.app/libkernrw.0.dylib
          cp ssh-keygen Payload/A13Exploit.app/ssh-keygen
          # 3. Gerar Info.plist necessária para o iOS reconhecer o App
          cat <<EOF > Payload/A13Exploit.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://apple.com">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key> <string>A13Exploit</string>
              <key>CFBundleIdentifier</key> <string>com.kernel.catalyst26</string>
              <key>CFBundleName</key> <string>Catalyst-26</string>
              <key>CFBundlePackageType</key> <string>APPL</string>
              <key>CFBundleShortVersionString</key> <string>1.0</string>
              <key>LSRequiresIPhoneOS</key> <true/>
              <key>MinimumOSVersion</key> <string>15.0</string>
              <key>CFBundleSupportedPlatforms</key> <array><string>iPhoneOS</string></array>
          </dict>
          </plist>
          EOF

      - name: Assinar com ldid (Bypass Sandbox & Root Privs)
        run: |
          brew install ldid
          cp sshd Payload/A13Exploit.app/sshd
          # [!] CRÍTICO: Assinar os binários com as entitlements que você postou
          # O arquivo deve se chamar exatamente 'ents.plist' no seu repositório
          if [ -f "ents.plist" ]; then
            ldid -Sents.plist -M Payload/A13Exploit.app/A13Exploit
            ldid -Sents.plist -M Payload/A13Exploit.app/sshd
            echo "[+] Assinatura ldid aplicada com sucesso."
          else
            echo "[!] ERRO: Arquivo ents.plist não encontrado. Usando assinatura simples."
            ldid -S Payload/A13Exploit.app/A13Exploit
            ldid -S Payload/A13Exploit.app/sshd
          fi
          
          # Garantir bits de execução
          chmod 755 Payload/A13Exploit.app/A13Exploit
          chmod 755 Payload/A13Exploit.app/sshd

      - name: Pack IPA
        run: |
          # O iOS exige que a pasta Payload seja zipada como .ipa
          zip -r A13Exploit.ipa Payload
          echo "[+] IPA Catalyst-26 Gerado."

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Catalyst-26-A13-IPA
          path: A13Exploit.ipa
